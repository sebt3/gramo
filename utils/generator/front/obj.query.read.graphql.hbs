{{#if namespaced}}
query {{ group }}{{ short }}Query($namespace: queryParameters, $obj: queryParameters{{#if detailed}}{{#each resolvers}}, ${{name}}: queryParameters{{/each}}{{/if}}) {
  k8sNamespace(params: $namespace) {
{{else}}
query {{ group }}{{ short }}Query($obj: queryParameters{{#if detailed}}{{#each resolvers}}, ${{name}}: queryParameters{{/each}}{{/if}}) {
{{/if}}
    {{ group }}{{ short }}(params: $obj) {
      metadata {
        name
{{#if namespaced}}
        namespace
{{/if}}
        annotations
        labels
        creationTimestamp
        uid
      }
      parentReference {
        name
        namespace
        group
        short
      }
{{#if detailed}}
  {{#each resolvers}}
      {{name}}(params: ${{name}}) {
    {{#if (or (eq group 'cnpg') (eq resultGroup 'cnpg'))}}
{{> cnpgCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'fluxcd') (eq resultGroup 'fluxcd'))}}
{{> fluxcdCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'k8s') (eq resultGroup 'k8s'))}}
{{> k8sCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'k8up') (eq resultGroup 'k8up'))}}
{{> k8upCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'monitoring') (eq resultGroup 'monitoring'))}}
{{> monitoringCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'redis') (eq resultGroup 'redis'))}}
{{> redisCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'secretgenerator') (eq resultGroup 'secretgenerator'))}}
{{> secretgeneratorCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'traefik') (eq resultGroup 'traefik'))}}
{{> traefikCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'vynil') (eq resultGroup 'vynil'))}}
{{> vynilCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'tekton') (eq resultGroup 'tekton'))}}
{{> tektonCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#if (or (eq group 'core') (eq resultGroup 'core'))}}
{{> coreCustom ctx=. operation='queryResolver'}}
    {{/if}}
    {{#unless (or (and (eq resultGroup 'vynil') (or (eq resultShort 'Package') (eq resultShort 'Category'))) (eq resultGroup 'core') )}}
        metadata {
          name
          namespace
          annotations
          labels
          creationTimestamp
          uid
        }
        getcoreProblem {
          source
          description
        }
        getcoreAdvice {
          source
          reason
          description
        }
      {{#each properties}}
        {{this}}
      {{/each}}
    {{/unless}}
      }
  {{/each}}
{{else}}
      getcoreProblem {
        source
        description
      }
    {{#if (eq group 'cnpg')}}
{{> cnpgCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'fluxcd')}}
{{> fluxcdCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'k8s')}}
{{> k8sCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'k8up')}}
{{> k8upCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'monitoring')}}
{{> monitoringCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'redis')}}
{{> redisCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'secretgenerator')}}
{{> secretgeneratorCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'traefik')}}
{{> traefikCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'vynil')}}
{{> vynilCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'tekton')}}
{{> tektonCustom ctx=. operation='query'}}
    {{/if}}
    {{#if (eq group 'core')}}
{{> coreCustom ctx=. operation='query'}}
    {{/if}}
{{/if}}
{{#each readProperties}}
      {{this}}
{{/each}}
    }
{{#if namespaced}}
  }
{{/if}}
}
