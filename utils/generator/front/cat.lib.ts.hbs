{{#each groups}}
export { isActive{{ ../category }}{{ name }} } from './{{ name }}/{{ ../category }}Cat.js'
{{/each}}
{{#each groups}}
import { isActive{{ ../category }}{{ name }} } from './{{ name }}/{{ ../category }}Cat.js'
{{/each}}
export function isActive{{ category }}() {
    let count = 0;
{{#each groups}}
    if (isActive{{ ../category }}{{ name }}()) count++;
{{/each}}
    return count>0;
}
