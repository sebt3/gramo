{{#with ctx}}
{{#if (and (eq ../operation 'extraComponents') (eq short 'Cluster'))}}"TabLogs",{{/if}}
{{#if (and (eq ../operation 'componentLoader') (eq short 'Cluster'))}}
    case "TabLogs":{
      return defineAsyncComponent(() => import( '@/components/k8s/ReplicaSetTabLogs.vue'))
    }
{{/if}}
{{#if (and (eq ../operation 'componentHave') (eq short 'Cluster'))}}
    (name != 'TabLogs' || Array.isArray(model.childk8sPod)) &&
{{/if}}
{{#if (and (eq ../operation 'queryResolver') (and (eq short 'Cluster') (and (eq resultGroup 'k8s') (eq resultShort 'Pod'))))}}
        childcoreContainer {
          name
          init
          getcoreLog {
            lines
          }
        }
{{/if}}
{{/with}}