mutation projectcalicoIPAMConfig($name: String!, $spec: JSONObject) {
  projectcalicoIPAMConfigPatch(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
