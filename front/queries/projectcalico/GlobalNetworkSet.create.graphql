mutation projectcalicoGlobalNetworkSet($name: String!, $spec: JSONObject) {
  projectcalicoGlobalNetworkSetCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
