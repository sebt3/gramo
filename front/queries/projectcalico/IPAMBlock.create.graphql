mutation projectcalicoIPAMBlock($name: String!, $spec: JSONObject) {
  projectcalicoIPAMBlockCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
