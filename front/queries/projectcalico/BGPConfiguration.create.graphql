mutation projectcalicoBGPConfiguration($name: String!, $spec: JSONObject) {
  projectcalicoBGPConfigurationCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
