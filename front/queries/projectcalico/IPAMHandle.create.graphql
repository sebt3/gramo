mutation projectcalicoIPAMHandle($name: String!, $spec: JSONObject) {
  projectcalicoIPAMHandleCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
