mutation projectcalicoFelixConfiguration($name: String!, $spec: JSONObject) {
  projectcalicoFelixConfigurationPatch(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
