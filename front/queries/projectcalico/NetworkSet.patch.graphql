mutation projectcalicoNetworkSet($namespace: String!, $name: String!, $spec: JSONObject) {
  projectcalicoNetworkSetPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
