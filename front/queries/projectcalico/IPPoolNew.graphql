
mutation ProjectcalicoIPPool($name: String!, $spec: projectcalicoIPPoolSpec!) {
  createProjectcalicoIPPool(name: $name, spec: $spec) {
    metadata {
      annotations {
        name
        value
      }
      labels {
        name
        value
      }
      name
      namespace
      obj
    }
    allowedUses
    blockSize
    cidr
    disableBGPExport
    disabled
    ipip {
      enabled
      mode
    }
    ipipMode
    nat-outgoing
    natOutgoing
    nodeSelector
    vxlanMode
  }
}
