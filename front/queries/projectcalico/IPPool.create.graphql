mutation projectcalicoIPPool($name: String!, $spec: JSONObject) {
  projectcalicoIPPoolCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
