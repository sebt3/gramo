mutation projectcalicoGlobalNetworkPolicy($name: String!, $spec: JSONObject) {
  projectcalicoGlobalNetworkPolicyCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
