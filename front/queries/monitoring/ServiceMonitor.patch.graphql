mutation monitoringServiceMonitor($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringServiceMonitorPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
