mutation monitoringPrometheusAgent($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringPrometheusAgentCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
