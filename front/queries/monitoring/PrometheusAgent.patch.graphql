mutation monitoringPrometheusAgent($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringPrometheusAgentPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
