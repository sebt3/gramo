mutation monitoringAlertmanagerConfig($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringAlertmanagerConfigCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
