mutation monitoringAlertmanager($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringAlertmanagerPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
