mutation monitoringPodMonitor($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringPodMonitorPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
