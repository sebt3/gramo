mutation monitoringPodMonitor($namespace: String!, $name: String!) {
  monitoringPodMonitorDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
