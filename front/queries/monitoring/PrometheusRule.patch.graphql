mutation monitoringPrometheusRule($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringPrometheusRulePatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
