mutation monitoringThanosRuler($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringThanosRulerPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
