mutation monitoringAlertmanager($namespace: String!, $name: String!, $spec: JSONObject) {
  monitoringAlertmanagerCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
