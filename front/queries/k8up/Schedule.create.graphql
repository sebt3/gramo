mutation k8upSchedule($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upScheduleCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
