mutation k8upArchive($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upArchiveCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
