mutation k8upSnapshot($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upSnapshotCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
