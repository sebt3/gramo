mutation k8upPreBackupPod($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upPreBackupPodCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
