mutation k8upCheck($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upCheckPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
