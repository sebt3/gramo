mutation k8upRestore($namespace: String!, $name: String!, $spec: JSONObject) {
  k8upRestoreCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
