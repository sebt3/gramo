mutation certmanagerIssuer($namespace: String!, $name: String!, $spec: JSONObject) {
  certmanagerIssuerPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
