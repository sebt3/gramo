mutation traefikTLSStore($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikTLSStoreCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
