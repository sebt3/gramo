mutation traefikMiddleware($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikMiddlewareCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
