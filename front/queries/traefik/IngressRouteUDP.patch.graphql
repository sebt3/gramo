mutation traefikIngressRouteUDP($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikIngressRouteUDPPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
