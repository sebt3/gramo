mutation traefikTLSOption($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikTLSOptionPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
