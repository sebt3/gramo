mutation traefikServersTransport($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikServersTransportPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
