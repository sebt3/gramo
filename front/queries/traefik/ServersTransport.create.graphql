mutation traefikServersTransport($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikServersTransportCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
