mutation traefikMiddleware($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikMiddlewarePatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
