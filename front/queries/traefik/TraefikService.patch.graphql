mutation traefikTraefikService($namespace: String!, $name: String!, $spec: JSONObject) {
  traefikTraefikServicePatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
