mutation fluxcdHelmRepository($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdHelmRepositoryPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
