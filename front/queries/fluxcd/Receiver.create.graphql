mutation fluxcdReceiver($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdReceiverCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
