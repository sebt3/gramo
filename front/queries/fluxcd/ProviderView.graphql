
query FluxcdProvider($namespace: String!, $name: String!) {
  customResourceDefinition(name: "providers.notification.toolkit.fluxcd.io") {
    versions {
      name
      served
      schema {
        openAPIV3Schema
      }
    }
    names {
      categories
      kind
      listKind
      plural
      singular
      shortNames
    }
    group
  }
  fluxcdProvider(namespace: $namespace, name: $name) {
    metadata {
      annotations {
        name
        value
      }
      labels {
        name
        value
      }
      name
      namespace
    }
    address
    certSecretRef
    channel
    interval
    proxy
    secretRef
    suspend
    timeout
    type
    username
    status {
      conditions
      lastHandledReconcileAt
      observedGeneration
    }
  }
}
