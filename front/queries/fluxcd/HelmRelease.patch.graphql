mutation fluxcdHelmRelease($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdHelmReleasePatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
