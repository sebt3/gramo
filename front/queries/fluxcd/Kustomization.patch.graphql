mutation fluxcdKustomization($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdKustomizationPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
