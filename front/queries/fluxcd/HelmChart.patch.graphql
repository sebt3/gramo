mutation fluxcdHelmChart($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdHelmChartPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
