mutation fluxcdImageRepository($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdImageRepositoryCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
