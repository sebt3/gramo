mutation fluxcdImagePolicy($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdImagePolicyPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
