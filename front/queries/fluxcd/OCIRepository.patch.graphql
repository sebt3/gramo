mutation fluxcdOCIRepository($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdOCIRepositoryPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
