mutation fluxcdProvider($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdProviderPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
