mutation fluxcdGitRepository($namespace: String!, $name: String!, $spec: JSONObject) {
  fluxcdGitRepositoryPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
