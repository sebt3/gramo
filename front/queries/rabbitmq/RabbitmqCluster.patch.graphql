mutation rabbitmqRabbitmqCluster($namespace: String!, $name: String!, $spec: JSONObject) {
  rabbitmqRabbitmqClusterPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
