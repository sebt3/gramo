mutation redisRedisCluster($namespace: String!, $name: String!, $spec: JSONObject) {
  redisRedisClusterPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
