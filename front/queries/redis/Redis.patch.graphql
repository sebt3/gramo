mutation redisRedis($namespace: String!, $name: String!, $spec: JSONObject) {
  redisRedisPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
