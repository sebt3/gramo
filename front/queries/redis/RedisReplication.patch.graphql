mutation redisRedisReplication($namespace: String!, $name: String!, $spec: JSONObject) {
  redisRedisReplicationPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
