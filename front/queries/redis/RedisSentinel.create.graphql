mutation redisRedisSentinel($namespace: String!, $name: String!, $spec: JSONObject) {
  redisRedisSentinelCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
