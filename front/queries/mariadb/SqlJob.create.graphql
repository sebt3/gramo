mutation mariadbSqlJob($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbSqlJobCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
