mutation mariadbMariaDB($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbMariaDBPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
