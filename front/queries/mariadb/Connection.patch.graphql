mutation mariadbConnection($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbConnectionPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
