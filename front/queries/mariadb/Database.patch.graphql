mutation mariadbDatabase($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbDatabasePatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
