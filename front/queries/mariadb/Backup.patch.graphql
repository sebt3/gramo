mutation mariadbBackup($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbBackupPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
