mutation mariadbGrant($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbGrantCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
