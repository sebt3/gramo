mutation mariadbRestore($namespace: String!, $name: String!, $spec: JSONObject) {
  mariadbRestoreCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
