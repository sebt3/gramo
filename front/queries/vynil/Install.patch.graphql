mutation vynilInstall($namespace: String!, $name: String!, $spec: JSONObject) {
  vynilInstallPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
