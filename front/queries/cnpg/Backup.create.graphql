mutation cnpgBackup($namespace: String!, $name: String!, $spec: JSONObject) {
  cnpgBackupCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
