mutation cnpgPooler($namespace: String!, $name: String!, $spec: JSONObject) {
  cnpgPoolerCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
