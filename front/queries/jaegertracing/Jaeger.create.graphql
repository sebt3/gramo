mutation jaegertracingJaeger($namespace: String!, $name: String!, $spec: JSONObject) {
  jaegertracingJaegerCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
