mutation secretgeneratorBasicAuth($namespace: String!, $name: String!, $spec: JSONObject) {
  secretgeneratorBasicAuthCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
