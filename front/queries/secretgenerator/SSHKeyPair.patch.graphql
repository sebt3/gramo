mutation secretgeneratorSSHKeyPair($namespace: String!, $name: String!, $spec: JSONObject) {
  secretgeneratorSSHKeyPairPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
