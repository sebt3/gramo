mutation secretgeneratorSSHKeyPair($namespace: String!, $name: String!, $spec: JSONObject) {
  secretgeneratorSSHKeyPairCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
