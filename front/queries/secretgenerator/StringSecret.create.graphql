mutation secretgeneratorStringSecret($namespace: String!, $name: String!, $spec: JSONObject) {
  secretgeneratorStringSecretCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
