mutation zalandopostgresql($namespace: String!, $name: String!, $spec: JSONObject) {
  zalandopostgresqlPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
