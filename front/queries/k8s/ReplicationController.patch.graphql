mutation k8sReplicationController($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sReplicationControllerPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
