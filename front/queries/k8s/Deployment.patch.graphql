mutation k8sDeployment($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sDeploymentPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
