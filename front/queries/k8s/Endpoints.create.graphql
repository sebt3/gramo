mutation k8sEndpoints($namespace: String!, $name: String!, $subsets: [JSONObject]) {
  k8sEndpointsCreate(namespace: $namespace, name: $name, subsets: $subsets) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      subsets
  }
}
