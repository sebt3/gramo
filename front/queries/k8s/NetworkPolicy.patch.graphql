mutation k8sNetworkPolicy($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sNetworkPolicyPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
