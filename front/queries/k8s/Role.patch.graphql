mutation k8sRole($namespace: String!, $name: String!, $rules: [JSONObject]) {
  k8sRolePatch(namespace: $namespace, name: $name, rules: $rules) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      rules
  }
}
