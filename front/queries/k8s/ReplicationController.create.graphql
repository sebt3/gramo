mutation k8sReplicationController($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sReplicationControllerCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
