mutation k8sPersistentVolumeClaim($namespace: String!, $name: String!) {
  k8sPersistentVolumeClaimDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
