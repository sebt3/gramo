mutation k8sHorizontalPodAutoscaler($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sHorizontalPodAutoscalerCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
