mutation k8sPersistentVolumeClaim($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sPersistentVolumeClaimCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
