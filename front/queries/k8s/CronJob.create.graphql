mutation k8sCronJob($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sCronJobCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
