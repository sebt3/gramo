mutation k8sPersistentVolume($name: String!, $spec: JSONObject) {
  k8sPersistentVolumeCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
