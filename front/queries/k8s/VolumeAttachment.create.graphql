mutation k8sVolumeAttachment($name: String!, $spec: JSONObject) {
  k8sVolumeAttachmentCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
