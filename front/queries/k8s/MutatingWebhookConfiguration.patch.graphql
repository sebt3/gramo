mutation k8sMutatingWebhookConfiguration($name: String!, $webhooks: [JSONObject]) {
  k8sMutatingWebhookConfigurationPatch(name: $name, webhooks: $webhooks) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      webhooks
  }
}
