mutation k8sPodDisruptionBudget($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sPodDisruptionBudgetPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
