mutation k8sStatefulSet($namespace: String!, $name: String!) {
  k8sStatefulSetDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
