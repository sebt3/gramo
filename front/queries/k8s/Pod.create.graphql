mutation k8sPod($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sPodCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
