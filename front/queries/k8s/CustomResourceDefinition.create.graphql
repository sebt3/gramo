mutation k8sCustomResourceDefinition($name: String!, $spec: JSONObject) {
  k8sCustomResourceDefinitionCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
