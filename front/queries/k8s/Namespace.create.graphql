mutation k8sNamespace($name: String!, $spec: JSONObject) {
  k8sNamespaceCreate(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
