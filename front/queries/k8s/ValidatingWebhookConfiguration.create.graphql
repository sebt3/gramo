mutation k8sValidatingWebhookConfiguration($name: String!, $webhooks: [JSONObject]) {
  k8sValidatingWebhookConfigurationCreate(name: $name, webhooks: $webhooks) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      webhooks
  }
}
