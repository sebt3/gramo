mutation k8sCSIDriver($name: String!, $spec: JSONObject) {
  k8sCSIDriverPatch(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
