mutation k8sCustomResourceDefinition($name: String!, $spec: JSONObject) {
  k8sCustomResourceDefinitionPatch(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
