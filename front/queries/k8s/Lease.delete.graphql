mutation k8sLease($namespace: String!, $name: String!) {
  k8sLeaseDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
  }
}
