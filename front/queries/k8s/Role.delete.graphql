mutation k8sRole($namespace: String!, $name: String!) {
  k8sRoleDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      rules
  }
}
