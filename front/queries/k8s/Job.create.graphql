mutation k8sJob($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sJobCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
