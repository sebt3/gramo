mutation k8sCertificateSigningRequest($name: String!, $spec: JSONObject) {
  k8sCertificateSigningRequestPatch(name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
