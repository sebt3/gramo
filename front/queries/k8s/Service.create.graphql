mutation k8sService($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sServiceCreate(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
