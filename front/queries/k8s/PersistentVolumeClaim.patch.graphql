mutation k8sPersistentVolumeClaim($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sPersistentVolumeClaimPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
