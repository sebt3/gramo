mutation k8sPod($namespace: String!, $name: String!, $spec: JSONObject) {
  k8sPodPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
