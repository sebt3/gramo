mutation k8sRoleBinding($namespace: String!, $name: String!) {
  k8sRoleBindingDelete(namespace: $namespace, name: $name) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      roleRef
      subjects
  }
}
