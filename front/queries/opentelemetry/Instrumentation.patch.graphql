mutation opentelemetryInstrumentation($namespace: String!, $name: String!, $spec: JSONObject) {
  opentelemetryInstrumentationPatch(namespace: $namespace, name: $name, spec: $spec) {
      metadata {
        name
        namespace
        annotations
        labels
        creationTimestamp
        uid
      }
      spec
      status
  }
}
