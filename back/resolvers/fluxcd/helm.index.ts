import { queries as HelmReleaseQueries } from './query.HelmRelease.js';
import { mutations as HelmReleaseMutations } from './mutation.HelmRelease.js';
export const queries = {
    ...HelmReleaseQueries,
};

export const resolvers = {
};

export const mutations = {
    ...HelmReleaseMutations,
};
