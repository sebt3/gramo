
import { queries as KustomizationQueries } from './query.Kustomization.js';
import { mutations as KustomizationMutations } from './mutation.Kustomization.js';
export const queries = {
    ...KustomizationQueries,
};

export const resolvers = {
};

export const mutations = {
    ...KustomizationMutations,
};
